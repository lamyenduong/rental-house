export const enum Colors {
  green = "#3E5E3F",
  white = "#FFFFFF",
  plants = "#5C821A",
  newgrowth = "#C6D166",
  blackish_gray = "#333333",
  light_gray = "#D9D9D9",
}

export const enum Authentication {
  isOwnerMode = "Owner",
  isTenantsMode = "Tenants",
}

export const enum AccessSession {
  isLogin = 0,
  isLogOut = 1,
}

export const enum ShowOrHide {
  show = 1,
  hide = 0,
}

export const enum Navigator {
  home = 0,
  statistics = 1,
  suites = 2,
  notifications = 3,
  available = 4,
  rented = 5,
}

export const enum Navigator2 {
  logo = 0,
  user = 1,
  message = 2,
  more = 3,
  search = 4,
  logout = 5,
}
